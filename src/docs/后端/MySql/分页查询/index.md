# 分页查询

```sql
select 字段 from 表名 limit m, n

-- m 表示从哪一条记录开始（从0开始计数）
-- n 表示查询多少条数据
-- 如果是分页的话，m 的计算方式是 (当前页码 - 1) * 每页显示数量
-- 例如，如果当前页是第 3 页，每页显示 10 条记录，则 m = (3 - 1) * 10 = 20
-- 这意味着查询将从第 21 条记录开始，获取接下来的 n 条记录
```